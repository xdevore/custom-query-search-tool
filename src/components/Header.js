import logo from './delaget image.png'

const Header = () => {
  return (
    <header className='Header'>
      <img className="App-logo" src={logo} alt="logo" />
    </header>
  )
};

export default Header;
